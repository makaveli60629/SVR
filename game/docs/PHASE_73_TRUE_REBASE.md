# SVR Poker Phase 73 — True Rebase

Base: `SVR-main.zip -> update/game.zip` (v241 clean rebuild).

Changes in this pack:
- original larger lobby baseline restored from the repo backup
- no shell-recovery/chat-only runtime mixed in
- preview-only pages added: `preview.html`, `cam3.html`
- preview mode hides HUD and locks to the auto-moving director camera
- build label updated to `20260331-P73-TRUE-REBASE`

This pack is a clean reset baseline intended for stable forward merges.
