# Reiki Hub Storefront Lock Rule

## Status
LOCKED until user explicitly says: `unlock the Reiki Hub`.

## Active visual baseline
The correct Reiki Hub storefront baseline is:

`UPDATE-3.0-PHASE-113-REIKI-1-4G-FRONT-CLEAN-LOCK`

This is the approved Reiki storefront/store hub front. It preserves the last good 1.4G-style front while removing the stacked overlay clutter from failed later Reiki rebuilds.

## New permanent rule
Do not touch the Reiki Hub store, Reiki storefront, Reiki mother module, Reiki hologram/video carousel button, Reiki private room route, Reiki video portal, Reiki plants, ropes, red carpet, teal trim, founder/info panels, right visual panel, center hologram slot, or Reiki carousel buttons unless the user explicitly says exactly:

`unlock the Reiki Hub`

## Protected visual target
The protected Reiki storefront includes:

- clean 1.4G-style Reiki storefront front
- wide teal storefront frame
- red carpet
- rope queue
- plant line and centerpiece plant
- left founder/about panel
- center portrait/hologram slot
- right visual panel
- stacked title/card panels above the center
- Reiki / Riki storefront signage
- small clean hologram carousel button strip
- working Reiki video portal route

## Hard block
Do not apply or rebuild from:

- Phase 111 failed backup
- Phase 112 retry if it changes the storefront front
- A-Frame 1.4H manifests
- A-Frame `index.html` / `world.js` replacements
- RICI rollback packets
- any package that stacks multiple Reiki overlays in front of the storefront

The current SVR game runtime is modular Three.js/WebXR. Do not replace it with a standalone A-Frame Reiki demo.

## Allowed while locked
Moon/Mars, Quest controls, poker, PGA, SVR Store, Smoker Lounge, buildings, ad tiers, and performance may be updated only if they do not alter or overwrite Reiki visuals or Reiki routes.

## Deployment safety
Every future game package must preserve this file. Any update that changes Reiki must be rejected unless the user first says exactly:

`unlock the Reiki Hub`

## Date
2026-06-06
