# Security Notes

- Do not paste secrets, API keys, database passwords, tokens, private credentials, or production keys into chat or frontend files.
- Use environment variables and a secure backend for SQL, Stripe, WebSocket, admin, and analytics systems.
- Keep diagnostics disabled in production by default.
