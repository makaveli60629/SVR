import * as THREE from 'three';
export function makeWatch({scene,renderer,camera,getState}){const watch=new THREE.Group();scene.add(watch);const tex=texture(getState());const body=new THREE.Mesh(new THREE.BoxGeometry(.42,.05,.23),new THREE.MeshStandardMaterial({color:0x09040f,roughness:.35,metalness:.25}));const screen=new THREE.Mesh(new THREE.PlaneGeometry(.36,.18),new THREE.MeshBasicMaterial({map:tex}));screen.rotation.x=-Math.PI/2;screen.position.y=.031;watch.add(body,screen);let last=0;function update(){const r=renderer.xr.getController(1);if(renderer.xr.isPresenting&&r){r.getWorldPosition(watch.position);r.getWorldQuaternion(watch.quaternion);watch.translateX(.08);watch.translateY(-.055);watch.translateZ(.04)}else{const p=new THREE.Vector3(.45,-.35,-.75).applyMatrix4(camera.matrixWorld);watch.position.copy(p);watch.quaternion.copy(camera.quaternion)}if(performance.now()-last>500){last=performance.now();draw(tex.image,getState());tex.needsUpdate=true}}return{update}}function texture(s){const c=document.createElement('canvas');c.width=512;c.height=256;draw(c,s);return new THREE.CanvasTexture(c)}function draw(c,s={}){const x=c.getContext('2d');x.fillStyle='#080010';x.fillRect(0,0,512,256);x.strokeStyle='#bc75ff';x.lineWidth=8;x.strokeRect(10,10,492,236);x.fillStyle='#e8dcff';x.font='bold 34px Arial';x.fillText('SVR WATCH',34,62);x.fillStyle='#caa6ff';x.font='24px Arial';x.fillText('Seat: '+(s.seat||'Standing'),34,114);x.fillText('Teleport: '+(s.teleport?'ON':'OFF'),34,154);x.fillStyle='#7fffdc';x.fillText('Cash: '+(s.cash||0),34,205)}