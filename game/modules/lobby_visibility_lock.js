import * as THREE from "three";

const REIKI_YAW = THREE.MathUtils.degToRad(348.93);
const COLORS = { reiki:"#7fffd4", pga:"#69e8ff", smoker:"#ff8bd7", store:"#ffd36b", scorpion:"#b48cff", sponsor:"#a7ff80" };
function makeLabelTexture(title, subtitle, color = "#69e8ff"){
  const c=document.createElement("canvas"); c.width=1024; c.height=512; const x=c.getContext("2d");
  x.clearRect(0,0,c.width,c.height); x.fillStyle="rgba(0,0,0,.32)"; x.fillRect(0,0,c.width,c.height); x.strokeStyle=color; x.lineWidth=10; x.strokeRect(28,28,c.width-56,c.height-56);
  x.textAlign="center"; x.textBaseline="middle"; x.shadowColor=color; x.shadowBlur=18; x.fillStyle="#fff"; x.font="900 76px Arial"; x.fillText(String(title).toUpperCase(),512,206);
  x.shadowBlur=7; x.fillStyle=color; x.font="800 35px Arial"; x.fillText(String(subtitle).toUpperCase(),512,324);
  const t=new THREE.CanvasTexture(c); t.colorSpace=THREE.SRGBColorSpace; t.anisotropy=8; t.needsUpdate=true; return t;
}
function makeLogoTexture(title, subtitle, color="#69e8ff", icon="SVR"){
  const c=document.createElement("canvas"); c.width=768; c.height=768; const x=c.getContext("2d"); x.clearRect(0,0,768,768);
  const g=x.createRadialGradient(384,338,30,384,338,360); g.addColorStop(0,color); g.addColorStop(.25,"rgba(255,255,255,.24)"); g.addColorStop(1,"rgba(0,0,0,0)"); x.fillStyle=g; x.fillRect(0,0,768,768);
  x.fillStyle="rgba(0,0,0,.58)"; x.beginPath(); x.arc(384,310,156,0,Math.PI*2); x.fill(); x.strokeStyle=color; x.lineWidth=12; x.beginPath(); x.arc(384,310,184,0,Math.PI*2); x.stroke();
  x.shadowColor=color; x.shadowBlur=18; x.fillStyle="#fff"; x.textAlign="center"; x.textBaseline="middle"; x.font="900 86px Arial"; x.fillText(icon,384,310); x.shadowBlur=7; x.font="900 50px Arial"; x.fillText(title.toUpperCase(),384,556); x.font="800 24px Arial"; x.fillStyle=color; x.fillText(subtitle.toUpperCase(),384,612);
  const t=new THREE.CanvasTexture(c); t.colorSpace=THREE.SRGBColorSpace; t.anisotropy=8; t.needsUpdate=true; return t;
}
function makeScanTexture(){ const c=document.createElement("canvas"); c.width=512; c.height=512; const x=c.getContext("2d"); x.clearRect(0,0,512,512); for(let y=0;y<512;y+=18){ x.fillStyle="rgba(127,255,212,.10)"; x.fillRect(0,y,512,1.5); } x.strokeStyle="rgba(127,255,212,.36)"; x.lineWidth=3; x.strokeRect(8,8,496,496); const t=new THREE.CanvasTexture(c); t.needsUpdate=true; return t; }
function makePlanetTexture(kind="moon"){
  const c=document.createElement("canvas"); c.width=2048; c.height=2048; const x=c.getContext("2d"); const main=x.createRadialGradient(670,560,58,1024,1024,1020);
  if(kind==="mars"){ main.addColorStop(0,"#c97347"); main.addColorStop(.25,"#74301f"); main.addColorStop(.58,"#2d0f0b"); main.addColorStop(1,"#080201"); } else { main.addColorStop(0,"#d5d9e2"); main.addColorStop(.28,"#747b88"); main.addColorStop(.60,"#252a34"); main.addColorStop(1,"#07090f"); }
  x.fillStyle=main; x.fillRect(0,0,2048,2048);
  if(kind==="mars"){ x.globalAlpha=.48; x.strokeStyle="#180604"; x.lineWidth=9; for(let i=0;i<52;i++){ const yy=135+i*40+Math.sin(i*1.55)*28; x.beginPath(); x.moveTo(65,yy); for(let xx=65;xx<1995;xx+=56)x.lineTo(xx,yy+Math.sin(xx*.012+i)*22); x.stroke(); } x.globalAlpha=.26; x.fillStyle="#b86543"; for(let i=0;i<160;i++){ x.beginPath(); x.ellipse(Math.random()*2048,Math.random()*2048,12+Math.random()*90,5+Math.random()*28,Math.random()*Math.PI,0,Math.PI*2); x.fill(); } x.globalAlpha=.34; x.fillStyle="#1b0704"; for(let i=0;i<120;i++){ x.beginPath(); x.ellipse(Math.random()*2048,Math.random()*2048,8+Math.random()*44,4+Math.random()*18,Math.random()*Math.PI,0,Math.PI*2); x.fill(); } }
  else { x.globalAlpha=.56; x.fillStyle="#121722"; for(let i=0;i<340;i++){ x.beginPath(); x.arc(Math.random()*2048,Math.random()*2048,6+Math.random()*76,0,Math.PI*2); x.fill(); } x.globalAlpha=.30; x.strokeStyle="#a8b0bd"; x.lineWidth=4; for(let i=0;i<125;i++){ x.beginPath(); x.arc(Math.random()*2048,Math.random()*2048,16+Math.random()*88,0,Math.PI*2); x.stroke(); } x.globalAlpha=.24; x.fillStyle="#d7dde8"; for(let i=0;i<90;i++){ x.beginPath(); x.arc(Math.random()*2048,Math.random()*2048,2+Math.random()*8,0,Math.PI*2); x.fill(); } }
  x.globalAlpha=1; const contrast=x.createLinearGradient(0,0,2048,2048); contrast.addColorStop(0,"rgba(255,255,255,.13)"); contrast.addColorStop(.38,"rgba(0,0,0,.02)"); contrast.addColorStop(1,"rgba(0,0,0,.72)"); x.fillStyle=contrast; x.fillRect(0,0,2048,2048);
  const vignette=x.createRadialGradient(660,560,230,1024,1024,1040); vignette.addColorStop(0,"rgba(255,255,255,.10)"); vignette.addColorStop(.58,"rgba(0,0,0,0)"); vignette.addColorStop(1,"rgba(0,0,0,.70)"); x.fillStyle=vignette; x.fillRect(0,0,2048,2048);
  const t=new THREE.CanvasTexture(c); t.colorSpace=THREE.SRGBColorSpace; t.anisotropy=8; t.needsUpdate=true; return t;
}
function makeVolumeTexture(volume=1,muted=false,gain=3.85,primed=false){ const c=document.createElement("canvas"); c.width=1000; c.height=520; const x=c.getContext("2d"); const pct=Math.round(volume*100); const boost=gain.toFixed(1); x.fillStyle="rgba(0,0,0,.86)"; x.fillRect(0,0,c.width,c.height); x.strokeStyle=muted?"#ff6b7f":COLORS.reiki; x.lineWidth=12; x.strokeRect(26,26,c.width-52,c.height-52); x.textAlign="center"; x.textBaseline="middle"; x.shadowColor=muted?"#ff6b7f":COLORS.reiki; x.shadowBlur=18; x.fillStyle="#fff"; x.font="900 46px Arial"; x.fillText("REIKI AUDIO",500,78); x.shadowBlur=7; x.fillStyle=muted?"#ff6b7f":COLORS.reiki; x.font="900 40px Arial"; x.fillText(muted?"MUTED":`VOL ${pct}% • BOOST ${boost}x`,500,148); x.fillStyle=primed?"#7fffd4":"#ffd36b"; x.font="900 27px Arial"; x.fillText(primed?"UNLOCKED":"CLICK GAME ONCE",500,210); x.fillStyle="rgba(255,255,255,.15)"; x.fillRect(130,260,740,44); x.fillStyle=muted?"#ff6b7f":COLORS.reiki; x.fillRect(130,260,Math.max(10,740*Math.min(1,gain/6)),44); x.fillStyle="#fff"; x.font="800 25px Arial"; x.fillText("[ lower ]  [ higher ]  U mute",500,362); x.fillStyle="#c9fff1"; x.font="700 22px Arial"; x.fillText("Small panel moved off glass view",500,418); const t=new THREE.CanvasTexture(c); t.colorSpace=THREE.SRGBColorSpace; t.anisotropy=8; t.needsUpdate=true; return t; }
function addPanel(root,title,subtitle,x,y,z,color,w=4.6,h=1.35){ const m=new THREE.Mesh(new THREE.PlaneGeometry(w,h),new THREE.MeshBasicMaterial({map:makeLabelTexture(title,subtitle,color),transparent:true,side:THREE.DoubleSide,depthWrite:false})); m.position.set(x,y,z); m.lookAt(0,y,0); root.add(m); return m; }
function addPortal(root,cfg){ const color=new THREE.Color(cfg.color).getHex(); const group=new THREE.Group(); group.position.set(cfg.x,0,cfg.z); group.userData.portalKey=cfg.key; root.add(group); const ring=new THREE.Mesh(new THREE.RingGeometry(.72,1.15,96),new THREE.MeshBasicMaterial({color,transparent:true,opacity:.62,side:THREE.DoubleSide,depthWrite:false})); ring.rotation.x=-Math.PI/2; ring.position.y=.045; group.add(ring); const disk=new THREE.Mesh(new THREE.CircleGeometry(1.10,96),new THREE.MeshBasicMaterial({color,transparent:true,opacity:.11,side:THREE.DoubleSide,depthWrite:false})); disk.rotation.x=-Math.PI/2; disk.position.y=.035; group.add(disk); const pillar=new THREE.Mesh(new THREE.CylinderGeometry(.08,.08,2.55,24,1,true),new THREE.MeshBasicMaterial({color,transparent:true,opacity:.16,side:THREE.DoubleSide,depthWrite:false})); pillar.position.y=1.28; group.add(pillar); const logo=new THREE.Mesh(new THREE.PlaneGeometry(1.95,1.95),new THREE.MeshBasicMaterial({map:makeLogoTexture(cfg.title,cfg.subtitle,cfg.color,cfg.icon),transparent:true,side:THREE.DoubleSide,depthWrite:false,opacity:.97})); logo.position.set(0,2.72,.02); if(cfg.rotationY!==undefined)logo.rotation.y=cfg.rotationY; else logo.lookAt(-cfg.x,2.72,-cfg.z); group.add(logo); const label=new THREE.Mesh(new THREE.PlaneGeometry(2.9,.78),new THREE.MeshBasicMaterial({map:makeLabelTexture(cfg.title,cfg.subtitle,cfg.color),transparent:true,side:THREE.DoubleSide,depthWrite:false,opacity:.94})); label.position.set(0,1.34,.03); if(cfg.rotationY!==undefined)label.rotation.y=cfg.rotationY; else label.lookAt(-cfg.x,1.34,-cfg.z); group.add(label); return group; }
function addRope(root,x1,z1,x2,z2){ const mat=new THREE.MeshBasicMaterial({color:0x8c1025}); const dx=x2-x1,dz=z2-z1,len=Math.hypot(dx,dz); const rope=new THREE.Mesh(new THREE.CylinderGeometry(.035,.035,len,16),mat); rope.position.set((x1+x2)/2,.82,(z1+z2)/2); rope.rotation.z=Math.PI/2; rope.rotation.y=Math.atan2(dx,dz); root.add(rope); for(const [x,z] of [[x1,z1],[x2,z2]]){ const pole=new THREE.Mesh(new THREE.CylinderGeometry(.055,.055,.95,16),new THREE.MeshBasicMaterial({color:0xd4af37})); pole.position.set(x,.48,z); root.add(pole); const cap=new THREE.Mesh(new THREE.SphereGeometry(.11,16,12),new THREE.MeshBasicMaterial({color:0xffd36b})); cap.position.set(x,.98,z); root.add(cap); } }
function addPlant(root,x,z){ const pot=new THREE.Mesh(new THREE.CylinderGeometry(.18,.24,.28,18),new THREE.MeshStandardMaterial({color:0x35180f,roughness:.9})); pot.position.set(x,.14,z); root.add(pot); const leaves=new THREE.Group(); leaves.position.set(x,.48,z); root.add(leaves); const mat=new THREE.MeshBasicMaterial({color:0x2d9f55,transparent:true,opacity:.86,side:THREE.DoubleSide}); for(let i=0;i<7;i++){ const leaf=new THREE.Mesh(new THREE.PlaneGeometry(.22,.72),mat); leaf.position.set(Math.sin(i)*.11,.26,Math.cos(i)*.11); leaf.rotation.y=i*Math.PI/3.5; leaf.rotation.x=.55; leaves.add(leaf); } }
function addConnector(){ /* removed old floor connector lines intentionally */ }
function addSolidBackdrops(root){ const wallMat=new THREE.MeshStandardMaterial({color:0x040711,roughness:.86,metalness:.02,emissive:0x080b14,emissiveIntensity:.16}); const trimMat=new THREE.MeshBasicMaterial({color:0x2b1248,transparent:true,opacity:.72}); const panels=[[-8.6,1.75,-9.88,4.2,3.5,.18],[0,1.75,-9.95,5.0,3.5,.18],[5.8,1.75,-9.88,4.2,3.5,.18],[9.75,1.65,.8,.18,3.3,4.4],[-9.75,1.65,.8,.18,3.3,4.4]]; for(const p of panels){ const m=new THREE.Mesh(new THREE.BoxGeometry(p[3],p[4],p[5]),wallMat); m.position.set(p[0],p[1],p[2]); root.add(m); const t=new THREE.Mesh(new THREE.BoxGeometry(p[3]+.08,.055,p[5]+.08),trimMat); t.position.set(p[0],p[1]+p[4]/2+.08,p[2]); root.add(t); } }
function addGlassHubExpansion(root){ const group=new THREE.Group(); group.name="SVR_Reiki_Expanded_Glass_Hub_Phase98S"; root.add(group); const glassMat=new THREE.MeshBasicMaterial({color:0x7fffd4,transparent:true,opacity:.135,side:THREE.DoubleSide,depthWrite:false}); const edgeMat=new THREE.MeshBasicMaterial({color:0x7fffd4,transparent:true,opacity:.48,depthWrite:false}); const floorMat=new THREE.MeshBasicMaterial({color:0x7fffd4,transparent:true,opacity:.08,side:THREE.DoubleSide,depthWrite:false}); const carpetMat=new THREE.MeshBasicMaterial({color:0x6e1026,transparent:true,opacity:.50,side:THREE.DoubleSide,depthWrite:false}); const floor=new THREE.Mesh(new THREE.PlaneGeometry(13.4,5.1),floorMat); floor.rotation.x=-Math.PI/2; floor.position.set(19.8,.025,-4.85); group.add(floor); const carpet=new THREE.Mesh(new THREE.PlaneGeometry(11.2,2.55),carpetMat); carpet.rotation.x=-Math.PI/2; carpet.position.set(20.0,.04,-4.95); group.add(carpet); const back=new THREE.Mesh(new THREE.PlaneGeometry(13.4,3.35),glassMat); back.position.set(19.8,1.88,-7.28); group.add(back); const left=new THREE.Mesh(new THREE.PlaneGeometry(4.85,3.05),glassMat); left.position.set(13.1,1.72,-4.85); left.rotation.y=Math.PI/2; group.add(left); const right=new THREE.Mesh(new THREE.PlaneGeometry(4.85,3.05),glassMat); right.position.set(26.5,1.72,-4.85); right.rotation.y=-Math.PI/2; group.add(right); const frontL=new THREE.Mesh(new THREE.PlaneGeometry(4.7,2.65),glassMat); frontL.position.set(16.15,1.50,-2.38); frontL.rotation.y=Math.PI; group.add(frontL); const frontR=frontL.clone(); frontR.position.x=23.45; group.add(frontR); const edges=[[19.8,3.58,-7.28,13.4,.055,.055],[19.8,.18,-7.28,13.4,.055,.055],[16.15,2.86,-2.38,4.7,.055,.055],[23.45,2.86,-2.38,4.7,.055,.055],[13.1,1.72,-4.85,.055,3.1,.055],[26.5,1.72,-4.85,.055,3.1,.055],[13.1,.18,-4.85,.055,.055,4.85],[26.5,.18,-4.85,.055,.055,4.85]]; for(const e of edges){ const m=new THREE.Mesh(new THREE.BoxGeometry(e[3],e[4],e[5]),edgeMat); m.position.set(e[0],e[1],e[2]); group.add(m); } addRope(group,14.6,-2.05,18.05,-2.05); addRope(group,21.55,-2.05,25.0,-2.05); addRope(group,14.6,-3.0,14.6,-6.7); addRope(group,25.0,-3.0,25.0,-6.7); addPlant(group,13.65,-2.85); addPlant(group,25.95,-2.85); addPlant(group,13.65,-6.75); addPlant(group,25.95,-6.75); addPanel(group,"Reiki Glass Showroom","Mirrored glass / clear entrance",19.8,3.9,-7.20,COLORS.reiki,6.4,1.1); return group; }
function addOrb(root,color,x,y,z,scale,kind){ const g=new THREE.Group(); g.position.set(x,y,z); root.add(g); const sphere=new THREE.Mesh(new THREE.SphereGeometry(scale,96,64),new THREE.MeshBasicMaterial({map:makePlanetTexture(kind),color:kind==="mars"?0xb16b50:0xbfc5d0})); g.add(sphere); const halo=new THREE.Mesh(new THREE.SphereGeometry(scale*1.15,64,32),new THREE.MeshBasicMaterial({color,transparent:true,opacity:kind==="mars"?.030:.038,side:THREE.BackSide,depthWrite:false})); g.add(halo); const light=new THREE.PointLight(color,kind==="mars"?.30:.36,210,1.6); g.add(light); return {group:g,sphere,halo}; }
function addReikiVideo(root){ const g=new THREE.Group(); g.position.set(20.35,1.82,-7.18); g.rotation.y=0; root.add(g); const video=document.createElement("video"); video.src="../site/assets/video/reiki_hologram.mp4"; video.muted=true; video.loop=true; video.playsInline=true; video.autoplay=true; video.preload="auto"; video.volume=1; video.play().catch(()=>{}); let primed=false,currentVolume=0,maxVolume=1,gainBoost=3.85,panel=null,muted=false,audioCtx=null,gainNode=null,mediaSource=null; const zoneCenter=new THREE.Vector3(20.35,1.6,-5.15); const refreshPanel=()=>{ if(!panel)return; const tex=makeVolumeTexture(maxVolume,muted,gainBoost,primed); panel.material.map?.dispose?.(); panel.material.map=tex; panel.material.needsUpdate=true; }; const setupAudio=()=>{ try{ const AC=window.AudioContext||window.webkitAudioContext; if(!AC)return false; if(!audioCtx)audioCtx=new AC(); if(audioCtx.state==="suspended")audioCtx.resume().catch(()=>{}); if(!mediaSource){ mediaSource=audioCtx.createMediaElementSource(video); gainNode=audioCtx.createGain(); mediaSource.connect(gainNode); gainNode.connect(audioCtx.destination); } return true; }catch(err){ console.warn("SVR Reiki audio boost setup failed",err); return false; } }; const setBoost=v=>{ gainBoost=THREE.MathUtils.clamp(v,.2,8); refreshPanel(); }; const primeAudio=()=>{ primed=true; setupAudio(); video.muted=muted; video.volume=1; video.play().catch(()=>{}); refreshPanel(); }; window.addEventListener("pointerdown",primeAudio,{once:true,passive:true}); window.addEventListener("keydown",primeAudio,{once:true}); window.addEventListener("keydown",e=>{ if(e.repeat)return; if(e.code==="BracketRight"||e.code==="Equal"){ muted=false; setBoost(gainBoost+.5); } if(e.code==="BracketLeft"||e.code==="Minus"){ setBoost(gainBoost-.5); } if(e.code==="KeyU"){ muted=!muted; refreshPanel(); } }); window.SVR_REIKI_AUDIO={primeAudio,setVolume:v=>{maxVolume=THREE.MathUtils.clamp(v,0,1);refreshPanel();},setBoost,getVolume:()=>maxVolume,getBoost:()=>gainBoost,mute:()=>{muted=true;refreshPanel();},unmute:()=>{muted=false;refreshPanel();},toggleMute:()=>{muted=!muted;refreshPanel();}}; const tex=new THREE.VideoTexture(video); tex.colorSpace=THREE.SRGBColorSpace; tex.minFilter=THREE.LinearFilter; tex.magFilter=THREE.LinearFilter; tex.generateMipmaps=false; tex.anisotropy=16; const wallGlow=new THREE.Mesh(new THREE.PlaneGeometry(4.15,2.45),new THREE.MeshBasicMaterial({color:0x7fffd4,transparent:true,opacity:.055,side:THREE.DoubleSide,depthWrite:false})); wallGlow.position.z=.010; g.add(wallGlow); const segments=7, totalW=3.55, segW=totalW/segments, curve=.34; for(let i=0;i<segments;i++){ const u=(i-(segments-1)/2)/((segments-1)/2); const screen=new THREE.Mesh(new THREE.PlaneGeometry(segW+.025,2.02),new THREE.MeshBasicMaterial({map:tex,side:THREE.DoubleSide,toneMapped:false,transparent:true,opacity:1,depthWrite:false})); screen.position.set(u*totalW/2,0,.05+Math.abs(u)*curve*.12); screen.rotation.y=-u*.18; g.add(screen); } const scan=new THREE.Mesh(new THREE.PlaneGeometry(3.68,2.05),new THREE.MeshBasicMaterial({map:makeScanTexture(),color:0x7fffd4,transparent:true,opacity:.055,side:THREE.DoubleSide,depthWrite:false})); scan.position.z=.09; g.add(scan); const ring=new THREE.Mesh(new THREE.RingGeometry(1.35,1.90,96),new THREE.MeshBasicMaterial({color:0x7fffd4,transparent:true,opacity:.35,side:THREE.DoubleSide,depthWrite:false})); ring.rotation.x=-Math.PI/2; ring.position.set(0,-1.78,1.10); g.add(ring); panel=new THREE.Mesh(new THREE.PlaneGeometry(2.35,1.24),new THREE.MeshBasicMaterial({map:makeVolumeTexture(maxVolume,muted,gainBoost,primed),transparent:true,side:THREE.DoubleSide,depthWrite:false,opacity:.97})); panel.position.set(14.0,1.75,-2.25); panel.rotation.y=THREE.MathUtils.degToRad(180); root.add(panel); return {group:g,video,zoneCenter,scan,volumePanel:panel,primeAudio,getState(){return {videoOn:!video.paused,primed,near:currentVolume>.004,volume:currentVolume,maxVolume,gainBoost,muted};},updateAudio(camera,dt=.016){ if(!camera)return; const p=new THREE.Vector3(); camera.getWorldPosition(p); const d=Math.hypot(p.x-zoneCenter.x,p.z-zoneCenter.z); const near=3.75,fade=8.0; let target=0; if(d<fade&&!muted)target=maxVolume*THREE.MathUtils.clamp((fade-d)/(fade-near),0,1); currentVolume=THREE.MathUtils.lerp(currentVolume,target,Math.min(1,dt*3.2)); if(gainNode)gainNode.gain.value=currentVolume*gainBoost; else video.volume=Math.min(1,currentVolume); video.muted=!primed||muted||currentVolume<.002; scan.material.opacity=.04+Math.sin(performance.now()*.006)*.012; if(video.paused)video.play().catch(()=>{}); }}; }
export function installLobbyVisibilityLock({scene}){ const root=new THREE.Group(); root.name="SVR_Phase98S_Showroom_Portals_Curved_Hologram"; scene.add(root); const reikiMain=new THREE.Vector3(-5.6,0,-9.15), reikiExact=new THREE.Vector3(20.35,0,-5.15); const portals=[{key:"reiki",label:"Reiki Main",target:"reiki",position:reikiMain.clone()},{key:"reikiExpanded",label:"Reiki Hologram Hub",target:"reiki",position:reikiExact.clone()},{key:"pga",label:"PGA",target:"pga",position:new THREE.Vector3(0,0,-9.25)},{key:"smoker",label:"Smoker Lounge",target:"sponsor",position:new THREE.Vector3(5.6,0,-9.15)},{key:"store",label:"SVR Store",route:"../site/store.html",position:new THREE.Vector3(-8.65,0,-8.05)},{key:"scorpion",label:"Scorpion Room",route:"./scorpion.html?v=phase98-playable",position:new THREE.Vector3(9.25,0,.8)}]; addSolidBackdrops(root); addGlassHubExpansion(root); addPortal(root,{key:"reiki",title:"Reiki",subtitle:"Main Portal",icon:"REI",color:COLORS.reiki,x:-5.6,z:-9.15}); addPortal(root,{key:"reikiExpanded",title:"Reiki",subtitle:"Hologram Hub",icon:"REI",color:COLORS.reiki,x:20.35,z:-5.15,rotationY:REIKI_YAW}); addPortal(root,{key:"pga",title:"PGA",subtitle:"Training",icon:"PGA",color:COLORS.pga,x:0,z:-9.25}); addPortal(root,{key:"smoker",title:"Smoker",subtitle:"Lounge",icon:"SMK",color:COLORS.smoker,x:5.6,z:-9.15}); addPortal(root,{key:"store",title:"SVR Store",subtitle:"North Wall",icon:"SVR",color:COLORS.store,x:-8.65,z:-8.05}); addPortal(root,{key:"scorpion",title:"Scorpion",subtitle:"Play Poker",icon:"SCP",color:COLORS.scorpion,x:9.25,z:.8}); addPortal(root,{key:"sponsor",title:"Sponsor",subtitle:"Ad Wall",icon:"AD",color:COLORS.sponsor,x:0,z:9.25}); const reikiVideo=addReikiVideo(root); addPanel(root,"Reiki Hologram","Curved wall display / audio boosted",20.35,4.35,-7.18,COLORS.reiki,5.4,1.25); addPanel(root,"Sponsor Board","Future partner surface",0,6.45,-11.35,COLORS.sponsor,6.5,1.9); addPanel(root,"Espresso With Cream","Tier 1 sponsor",8.9,6.2,-5.35,"#ffb477",4.6,1.55); addPanel(root,"SVR Store","North wall portal",-8.65,5.65,-9.60,COLORS.store,4.6,1.45); addPanel(root,"Scorpion Room","Playable poker now open",8.9,5.4,5.35,COLORS.scorpion,4.6,1.55); addPanel(root,"Play With Purpose","Community impact",0,6.45,11.35,COLORS.pga,6.5,1.9); const moon=addOrb(root,0x7898d8,-10,92,-156,9.8,"moon"); const mars=addOrb(root,0xd64c2b,28,84,-178,6.1,"mars"); return {portals,reikiVideo,primeReikiAudio:reikiVideo.primeAudio,getReikiAudioState:reikiVideo.getState,update(t=0,dt=.016){ const cam=scene.userData?._camera; reikiVideo.updateAudio(cam,dt); moon.group.position.set(-10+Math.sin(t*.010)*9.0,92+Math.sin(t*.007)*.85,-156+Math.cos(t*.010)*5.8); moon.sphere.rotation.y+=dt*.026; moon.halo.rotation.y+=dt*.010; mars.group.position.set(28+Math.sin(t*.009)*9.2,84+Math.sin(t*.006)*.7,-178+Math.cos(t*.009)*6.3); mars.sphere.rotation.y+=dt*.048; mars.halo.rotation.y+=dt*.014; }}; }
