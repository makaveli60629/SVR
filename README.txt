SCARLETTVR POKER • MASTER ZIP
PERMANENT STRUCTURE:
- Root bootstrap: index.js (imports js/scarlett1/init.js ONLY)
- All active work happens inside: /js/scarlett1/
- Assets:
  - assets/models/combat_ninja.glb (your uploaded model)
  - assets/textures/* (generated)
Deploy:
- Upload ALL files/folders to GitHub Pages repo root.
- Ensure Pages is set to serve from the root.
