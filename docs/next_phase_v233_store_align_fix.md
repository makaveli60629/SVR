store alignment fix v2.3.3

- moved the store sign panel left so it no longer sits behind the store model
- mounted store.fbx on the right side of the northeast wall
- re-centered the store model from mesh bounds before placement
- reduced store model size for wall fit
- rotated the store model so it faces the table instead of sticking out edge-on
- moved the active button and label to the store sign side
