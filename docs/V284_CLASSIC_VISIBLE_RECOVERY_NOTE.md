V284 classic visible recovery
- replaced the fragile module boot path with a single-file classic scene
- keeps one lobby only
- local assets only inside game.zip
- includes table.glb, moon textures, mars textures, felt, and center logo
- adds inline VR button and music button
- avoids the split embed/autocam behavior that caused mixed scenes
