# store wall refine v2.3.4

- split the north-east store wall into a left sign panel and right model bay
- added a wall-facing orientation chooser for `store.fbx`
- centered the store model using mesh bounds instead of the raw fbx pivot
- reduced the store model scale so it fits the wall better
- aligned the store model to face inward toward the table more cleanly
- moved the sign/button stack farther left to avoid overlap with the model
