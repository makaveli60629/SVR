# v2.2.8 arms + watch cleanup

- removed the three.js XR hand-model mesh layer to avoid duplicate hands
- kept joint tracking so teleport/watch interactions still have tracked wrist/finger data
- kept `arms.fbx` as the watch mount base
- moved the watch closer to the wrist/forearm anchor
- fixed the watch UI to stay outward instead of flipping inward dynamically
- slightly tightened the cuff/band positions around the forearm mount
- game-only patch, site untouched
