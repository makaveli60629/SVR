# Phase 203 Backend Note

Optional backend route/table for bridge recorder event diagnostics. This is not required for the game to boot. The browser bridge queues telemetry safely even when the backend is offline.
