const SVR_PWA_CACHE='svr-poker-pwa-phase378-emergency-v1';
const SVR_CORE_ASSETS=['/','/index.html','/manifest.webmanifest','/offline.html','/logo.png','/logo.webp','/downloads/','/downloads/index.html','/game/android-stable.html?v=phase378','/game/android.html?v=phase378','/update/app-version.json','/game/android-release.json'];
self.addEventListener('install',event=>event.waitUntil(caches.open(SVR_PWA_CACHE).then(cache=>cache.addAll(SVR_CORE_ASSETS.map(url=>new Request(url,{cache:'reload'}))).catch(()=>undefined)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==SVR_PWA_CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('message',event=>{if(event?.data?.type==='SKIP_WAITING')self.skipWaiting();});
async function networkFirst(request){const cache=await caches.open(SVR_PWA_CACHE);try{const response=await fetch(new Request(request,{cache:'no-store'}));if(response?.ok&&!/\.apk(?:$|\?)/i.test(request.url))cache.put(request,response.clone()).catch(()=>undefined);return response;}catch(error){const cached=await cache.match(request,{ignoreSearch:false})||await caches.match(request);if(cached)return cached;if(request.mode==='navigate')return cache.match('/offline.html');throw error;}}
self.addEventListener('fetch',event=>{const request=event.request;if(request.method!=='GET')return;const url=new URL(request.url);if(url.origin!==self.location.origin)return;if(request.mode==='navigate'||url.pathname.startsWith('/game/')||url.pathname.startsWith('/downloads/')||url.pathname.startsWith('/update/')||url.pathname.endsWith('.apk')||url.pathname==='/'||url.pathname==='/index.html'||/deploy-health\.json|android-release\.json|app-version\.json/i.test(url.pathname)){event.respondWith(networkFirst(request));return;}event.respondWith(caches.match(request).then(cached=>cached||networkFirst(request)));});