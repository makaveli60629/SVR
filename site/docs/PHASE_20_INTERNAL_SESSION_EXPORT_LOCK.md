# Site Internal Phase 20 — Session Export Lock

Adds a small internal JavaScript helper for reading exported game QA/session JSON.

Protected files not touched:
- root `index.html`
- Matrix rain files
- public launch styling
- `/game`
