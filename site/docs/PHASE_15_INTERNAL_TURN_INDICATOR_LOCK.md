# Site Internal Phase 15 — Turn Indicator Lock

Scope: internal `/site` helpers only. Root public Matrix launch page is untouched.

Adds `site/js/turn-indicator-client.js` for future dashboard/watch telemetry review.
