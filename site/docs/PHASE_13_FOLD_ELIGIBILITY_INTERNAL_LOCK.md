# Site Phase 13 — Fold Eligibility Internal Lock

Build: `PHASE-183-FOLD-ELIGIBILITY-MUCK-LOCK`

Adds internal `/site` browser hooks for folded/mucked player eligibility events.

Protected:

- Root public Matrix launch page is untouched.
- `/game` changes are handled separately.
- No API keys, SQL strings, Stripe secrets, or passwords.
