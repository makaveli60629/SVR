# Site Internal Phase 12 — Side Pot Internal Lock

Build: `PHASE-182-SIDE-POT-ELIGIBILITY-LOCK`

Adds an internal `/site/js/side-pot-client.js` listener only. It does not touch root public `index.html`, Matrix rain, public CSS, or `/game` directly.
