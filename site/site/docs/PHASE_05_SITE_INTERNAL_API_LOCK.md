# Phase 05 Site Internal API Lock

Build: PHASE-175-CLEAN-RUNTIME-POKER-DB-LOCK

This patch adds internal `/site/` API hooks and an admin check page only.
It does not include root `index.html`, Matrix scripts, public CSS, logo files, or `/game` files.
