(() => { const out = document.getElementById('out'); async function load(){ try { const base = localStorage.getItem('svr_api_base') || ''; if(!base){ out.textContent = 'Set localStorage.svr_api_base to backend URL to load bridge proxy reports.'; return; } const res = await fetch(base + '/api/game/bridge-proxy?limit=20', {cache:'no-store'}); out.textContent = JSON.stringify(await res.json(), null, 2); } catch(e){ out.textContent = e.stack || String(e); } } load(); })();