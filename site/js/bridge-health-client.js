async function loadReports(){const out=document.getElementById('out');out.textContent='Loading bridge health reports...';try{const base=localStorage.getItem('svr_api_base')||'';if(!base){out.textContent='No API base set. In console: localStorage.setItem("svr_api_base","https://YOUR-API")';return;}const r=await fetch(base+'/api/game/bridge-health?limit=30',{cache:'no-store'});out.textContent=JSON.stringify(await r.json(),null,2);}catch(e){out.textContent='Bridge health load failed: '+(e.message||e);}}