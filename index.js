// SVR root entry (NO bare imports)
// Loads runtime spine which loads the Scarlett1 world.
// Permanent folder layout: /SVR/js/runtime + /SVR/js/scarlett1
import { Spine } from "./js/runtime/spine.js";

Spine.start();
