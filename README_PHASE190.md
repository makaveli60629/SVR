# SVR Phase 190 Backend Starter

Adds session export storage endpoints for QA/playtest reports.

Routes:
- `POST /api/game/session-export`
- `GET /api/game/session-export?limit=30`

No secrets included. Keep SQL connection strings in environment settings only.
