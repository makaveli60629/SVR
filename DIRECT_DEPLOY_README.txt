SVR Site Phase 03 Refined Direct Deploy

Use the included APPLY-SITE-PHASE03-REFINED.ps1 script.

This package is site-only and does not touch /game.

Important: the current GitHub workflow excludes zip files and the update folder, so uploading only site.zip or update/site.zip will not change the live site. The direct files must be committed.

Cash App handle locked to: $SVRhelp
