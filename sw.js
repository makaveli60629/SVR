const SVR_CACHE='svr-poker-v1-0-0';
const CORE=['/offline.html','/logo.png','/manifest.webmanifest?v=1.0.0','/game/app.html?v=1.0.0','/game/styles/app-v1.css?v=1.0.0','/game/modules/app-v1.js?v=1.0.0','/game/data/tournaments-v1.json','/update/app-v1-release.json'];
self.addEventListener('install',event=>event.waitUntil(caches.open(SVR_CACHE).then(cache=>cache.addAll(CORE.map(url=>new Request(url,{cache:'reload'}))).catch(()=>undefined)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==SVR_CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('message',event=>{if(event?.data?.type==='SKIP_WAITING')event.waitUntil(self.skipWaiting())});
const alwaysFresh=url=>url.pathname.startsWith('/update/')||url.pathname.endsWith('/deploy-health.json')||url.pathname.endsWith('.apk');
async function networkFirst(request){try{return await fetch(new Request(request,{cache:'no-store'}))}catch(error){const cached=await caches.match(request,{ignoreSearch:true})||await caches.match('/offline.html');if(cached)return cached;throw error}}
self.addEventListener('fetch',event=>{const request=event.request;if(request.method!=='GET')return;const url=new URL(request.url);if(url.origin!==self.location.origin)return;if(alwaysFresh(url)){event.respondWith(fetch(new Request(request,{cache:'no-store'})));return}if(request.mode==='navigate'){event.respondWith(networkFirst(request));return}event.respondWith(caches.match(request,{ignoreSearch:true}).then(cached=>cached||networkFirst(request)))});
